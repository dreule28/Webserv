[x] - CGI stdin: pass POST body via stdin (second pipe needed)
[] - QUERY_STRING: strip from request path, pass as env var to CGI
[] - custom error pages: add errorPages map to ServerConfig, use in errorResponse()
[] - add missing CGI env vars: PATH_INFO, SCRIPT_NAME, SERVER_NAME, SERVER_PORT, SERVER_PROTOCOL, GATEWAY_INTERFACE, HTTP_* headers
[x] - change directory before calling execve in the child
[x] - CGI interpreter: use loc->cgiPath as the execve binary, not the script itself
[x] - CGI output parsing: split output at blank line, extract CGI headers (Content-Type, Status:) before setting body
[] - CGI blocking I/O: integrate CGI pipe FDs into epoll loop (grade 0 risk per subject)
[x] - waitpid exit status: use WIFEXITED(status) && WEXITSTATUS(status) == 0
[x] - NULL check on routeMatching result before dereferencing loc (segfault risk)